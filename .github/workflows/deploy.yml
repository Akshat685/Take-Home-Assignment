# ============================================================
# DISABLED — EC2 instance was deleted
# Kept for reference / learning purposes
# ============================================================
#
# # ============================================================
# # Deploy to EC2 — Automatic deployment on push to main
# # ============================================================
# name: Deploy to EC2
#
# on:
#   # Run after the Full Pipeline completes successfully
#   workflow_run:
#     workflows: ["Full Pipeline"]
#     types: [completed]
#     branches: [main]
#
# jobs:
#   deploy:
#     name: "🚀 Deploy to EC2"
#     runs-on: ubuntu-latest
#
#     # Only deploy if the pipeline succeeded (not on failure)
#     if: ${{ github.event.workflow_run.conclusion == 'success' }}
#
#     steps:
#       - name: Deploy via SSH
#         uses: appleboy/ssh-action@v1
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ${{ secrets.EC2_USERNAME }}
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             # Pull the latest image
#             docker pull ghcr.io/akshat685/take-home-assignment:latest
#
#             # Stop and remove the old container
#             docker stop chat-rag-app || true
#             docker rm chat-rag-app || true
#
#             # Start a new container with the updated image
#             docker run -d \
#               -p 3000:3000 \
#               --env-file /home/ec2-user/.env \
#               --name chat-rag-app \
#               --restart unless-stopped \
#               ghcr.io/akshat685/take-home-assignment:latest
#
#             # Clean up old images to save disk space
#             docker image prune -f
#
#             echo "✅ Deployment complete!"
#
#       - name: Deployment Summary
#         run: |
#           echo "## Deployment Results 🚀" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "✅ Successfully deployed to EC2" >> $GITHUB_STEP_SUMMARY
#           echo "🌍 App URL: http://${{ secrets.EC2_HOST }}" >> $GITHUB_STEP_SUMMARY
